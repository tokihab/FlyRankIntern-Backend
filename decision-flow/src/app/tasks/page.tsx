"use client";

import { useEffect, useState } from "react";
import { Plus, RefreshCw, Trash2 } from "lucide-react";

export default function TasksPage() {
  const [tasks, setTasks] = useState<Array<{ id: number; title: string; done: boolean }>>([]);
  const [title, setTitle] = useState("");
  const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [authMode, setAuthMode] = useState<"login" | "signup">("login"); const [session, setSession] = useState<string | null>(null);
  const [message, setMessage] = useState("Loading tasks...");
  const load = async () => { const response = await fetch("/api/backend/tasks", { cache: "no-store" }); const data = await response.json(); setTasks(Array.isArray(data) ? data : []); setMessage(response.ok ? "SQLite task store connected" : data.error ?? "Backend unavailable"); };
  useEffect(() => { void load(); }, []);
  const authenticate = async () => { const response = await fetch(`/api/backend/auth/${authMode === "login" ? "login" : "signup"}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, password }) }); const data = await response.json(); if (response.ok && data.access_token) { localStorage.setItem("flyrank_access_token", data.access_token); setSession(data.access_token); setMessage("Supabase session active"); } else if (response.ok) setMessage("Account created; check Supabase confirmation settings."); else setMessage(data.error ?? "Authentication failed"); };
  const add = async () => { if (!title.trim()) return; await fetch("/api/backend/tasks", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title }) }); setTitle(""); void load(); };
  const remove = async (id: number) => { await fetch(`/api/backend/tasks/${id}`, { method: "DELETE" }); void load(); };
  return <section className="mx-auto max-w-6xl px-6 py-10"><Header title="Tasks & Auth" subtitle="Supabase-protected identity and the original SQLite CRUD surface." /><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><p className="text-xs uppercase tracking-[0.2em] text-slate-500">Session boundary</p><h2 className="mt-4 text-xl font-medium">Supabase Auth</h2><div className="mt-4 space-y-2"><input value={email} onChange={(event) => setEmail(event.target.value)} placeholder="Email" type="email" className="w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2 text-sm" /><input value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Password" type="password" className="w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2 text-sm" /><div className="flex gap-2"><button onClick={() => void authenticate()} className="rounded-lg bg-emerald-300 px-3 py-2 text-sm text-slate-950">{authMode === "login" ? "Log in" : "Sign up"}</button><button onClick={() => setAuthMode(authMode === "login" ? "signup" : "login")} className="rounded-lg border border-white/10 px-3 py-2 text-sm">Switch to {authMode === "login" ? "sign up" : "login"}</button></div></div><p className="mt-4 text-sm leading-6 text-slate-400">{session ? "JWT stored for this browser session." : "Authentication is owned by the Express API; the token is kept locally for protected calls."}</p><div className="mt-6 rounded-lg border border-emerald-300/20 bg-emerald-300/[0.06] p-3 text-sm text-emerald-200">{message}</div></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[0.2em] text-slate-500">Task database</p><h2 className="mt-2 text-xl font-medium">SQLite CRUD</h2></div><button onClick={() => void load()} className="rounded-lg border border-white/10 p-2 text-slate-400 hover:text-white" aria-label="Refresh tasks"><RefreshCw size={16} /></button></div><div className="mt-5 flex gap-2"><input value={title} onChange={(event) => setTitle(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") void add(); }} placeholder="New task title" className="min-w-0 flex-1 rounded-lg border border-white/10 bg-slate-950 px-3 py-2 text-sm outline-none focus:border-emerald-300" /><button onClick={() => void add()} className="rounded-lg bg-emerald-300 px-3 text-slate-950" aria-label="Add task"><Plus size={17} /></button></div><div className="mt-5 space-y-2">{tasks.map((task) => <div key={task.id} className="flex items-center justify-between rounded-lg border border-white/10 px-3 py-3 text-sm"><span>{task.title}</span><button onClick={() => void remove(task.id)} className="text-slate-500 hover:text-rose-300" aria-label={`Delete ${task.title}`}><Trash2 size={16} /></button></div>)}{tasks.length === 0 && <p className="py-6 text-center text-sm text-slate-500">No tasks returned.</p>}</div></div></div></section>;
}

function Header({ title, subtitle }: { title: string; subtitle: string }) { return <div className="mb-8"><p className="text-xs uppercase tracking-[0.22em] text-emerald-300">Unified service</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">{title}</h1><p className="mt-3 text-slate-400">{subtitle}</p></div>; }
